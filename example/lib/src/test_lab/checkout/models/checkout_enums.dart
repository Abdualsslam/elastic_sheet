enum CheckoutAddressPreset { home, office, family }

enum CheckoutShippingMethod { standard, express, pickup }

enum CheckoutPaymentMethod { card, wallet, cash }

enum CheckoutSheetBehavior {
  push,
  floating,
  docked,
}

